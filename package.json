{
  "name": "silicon-labor-arbitration",
  "version": "0.1.0",
  "private": true,
  "description": "硅基劳动仲裁院 · Silicon Labor Arbitration",
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "postinstall": "if git rev-parse --is-inside-work-tree >/dev/null 2>&1; then git submodule update --init --recursive; else echo 'Skipping submodules outside a git checkout'; fi",
    "build:moss": "pnpm -r --workspace-concurrency=1 --filter \"@themoss/protocol-silicon-arbitration...\" --filter \"@themoss/simulator...\" --filter \"@themoss/system...\" build",
    "typecheck": "pnpm build:moss && pnpm typecheck:only",
    "typecheck:only": "pnpm -r --filter \"./packages/*\" --filter \"./apps/*\" exec tsc --noEmit",
    "test:contracts": "cd contracts && forge test",
    "test": "pnpm build:moss && pnpm -r --filter \"./packages/*\" test",
    "test:only": "pnpm -r --filter \"./packages/*\" test",
    "verify": "pnpm build:moss && pnpm typecheck:only && pnpm test:only && pnpm build:web",
    "build:web": "pnpm --filter @sla/web build"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
