# D1 日报 · RISO · 2026-07-30

## 今天完成

| | |
|---|---|
| 审核并合并 Neo 的两个 PR | 产品架构文档 + Moss Testnet Runtime |
| `packages/domain` | 案件数据结构、8 状态机、校验器、土豆案样例 |
| `packages/rules` | 确定性规则引擎，按权重推导结算 |
| `packages/ai` | AI 三路意见，接 Gonka，已跑通真实调用 |

**三个关键决定：**

1. **合约从 4 个塌到 2 个**（`TaskEscrow` + `CaseRegistry`）—— 同意 Neo 的建议。跨合约权限是 4 合约方案唯一的真风险面，提前删掉比等 D3 检查稳。
2. **引入 `weightBps`（条款权重）** —— `0.15 / 0.05` 之前是写死的魔术数字，评委问"为什么冻结 0.05"答不上来。现在由事前承诺的 25% 权重算出，且必须在 `createTask` 前进 `requirementsHash`，争议后改不动。
3. **AI 意见固化，路演不现场调 API** —— 网络、额度、模型拒答任何一个出问题都会卡在最关键的一页。

## 三条产品不变量已经写成代码

不再只是文档里的约定，违反会直接报错：

- **C4 不许被判** —— 主观条款被判成 `satisfied`/`violated` → 校验报 P0
- **AI 不许决定钱** —— 输出 schema 里根本没有金额字段，输入也剔除了金额和权重
- **AI 意见必须引用证据** —— `cites` 为空、引用假编号、正文方括号不是证据编号，全部拒绝并重试

---

## 给 NEO

**已同意 / 已配合：**
- 合约塌到 `TaskEscrow` + `CaseRegistry` 两个
- `MossPreSignEvidence`（E3）字段已按 `docs/08` §4.3 建好，语义映射三件套都在，样例里是 `"PENDING"` 占位，**等你填**
- `weightBps` 放在链下 canonical requirements JSON 里由 `requirementsHash` 承诺，**合约侧不用动**——仍然只校验 `toWorker + toClient + frozen == escrowedAmount`

**两个提醒：**
1. 写 `silicon-arbitration` Protocol 时**记得加 `chainId: 10143`**。你 Runtime PR 里 Registry 的检查是"声明了才校验"，漏写会静默放行——而我们这个是绑定部署地址的，正是那道守卫要防的类型。
2. Internet Court 日期：你引的 7/2 是新闻稿内文落款，但**发布和所有媒体报道都是 7/10**。路演用 7/10 或"三周前"。

**我卡在你这：** D2 的 wagmi hooks 封装要等 Gate 3（`TaskEscrow.createTask` 接口 + `TaskCreated` 事件冻结）。你交了 ABI 我就能接。

---

## 给 ELEVEN

`git pull` 之后 `packages/domain` 就能用，看那个目录的 README。

```ts
import { freshPotatoCase, CASE_STATUS_LABEL, hasUndecidable, undecidableIds } from "@sla/domain";
const c = freshPotatoCase();   // 完整数据，可反复重置
```

**四件和你直接相关的：**

1. **AI 三路意见是真实生成后固化的**，不用等 API。每份 100–130 字，三栏并排不会溢出。
2. **正文里的 `[E1]` `[E2]` `[E3]` 是可点击的证据角标** —— 点了应高亮 `c.evidence` 里对应那条。方括号里保证只有真实证据编号，有校验拦着。
3. **空章位判断：** `hasUndecidable(c)` → `true`，`undecidableIds(c)` → `["C4"]`。
4. **金额是算出来的：** `c.settlementProposal` = `0.15 / 0 / 0.05`，来自四条验收条件各 25% 的事前权重。

⚠️ **别依赖 `c.onchain` 里的字段细节**，等 Neo 冻结合约接口。上面这些链下数据是冻结的，按它做不会返工。

---

## 待办

| | |
|---|---|
| D2 wagmi hooks 封装 | **阻塞**，等 Neo 的 ABI |
| prompt 小改进 | 提醒检方去看责任链的 `sawWarning`（对比 MiniMax 时发现的）。不急，下次改 prompt 时一起 |
| 校验器 `cites` 去重 | P2，不影响 demo |
| `AGENTS.md` 的 `ELEVENT` | 已改成 `ELEVEN` |
