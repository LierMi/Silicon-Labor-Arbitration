# D1 日报 · RISO · 2026-07-30

## 我产出了什么

三个包，已合并进 main，`git pull` 就能用。

| | 是什么 | 谁会用 |
|---|---|---|
| `packages/domain` | 案件数据结构、8 状态机、校验器、土豆案样例 | **Eleven 全部**、Neo 的 E3 |
| `packages/rules` | 确定性规则引擎，按权重推导结算 | 我 |
| `packages/ai` | AI 三路意见，已接 Gonka 真实跑通并固化 | 我 |

另外审核并合并了 Neo 的四个 PR（结论都在各 PR 里，不在这里复述）。

## 三条产品不变量已经写成代码

不再只是文档里的约定，违反会直接报错：

- **C4 不许被判** —— 主观条款被判成 `satisfied`/`violated` → 校验报 P0
- **AI 不许决定钱** —— 输出 schema 里根本没有金额字段，输入也剔除了金额和权重
- **AI 意见必须引用证据** —— `cites` 为空、引用假编号、正文方括号不是证据编号，全部拒绝并重试

## 两个决定（会影响你们）

**1. 引入条款权重 `weightBps`**

`0.15 / 0.05` 之前是写死的数字，评委问"为什么冻结 0.05"答不上来。现在由四条验收条件各 25% 的事前权重算出，且必须在 `createTask` 前进 `requirementsHash`，争议后改不动。

**2. AI 意见固化，路演不现场调 API**

三份意见已真实生成并写进 fixture，`freshPotatoCase().aiArguments` 直接就有。网络、额度、模型拒答任何一个出问题都会卡在最关键的一页。
我接入了 Gonka API 模型 Kimi-k2.6测试用，免费额度但智力和稳定性一般，有更好的可改。

---

## 给 NEO

**已配合，你不用动：**
- `weightBps` 放在链下 canonical requirements JSON 里由 `requirementsHash` 承诺，合约仍然只校验 `toWorker + toClient + frozen == escrowedAmount`
- `MossPreSignEvidence`（E3）字段已按 `docs/08` §4.3 建好，语义映射三件套都在，样例里是 `"PENDING"` 占位，**等你填**

## 给 ELEVEN

单独写了一份：[`给ELEVEN-D2同步.md`](./给ELEVEN-D2同步.md)，末尾有一段可以直接复制给 AI 助手。

一句话版：**数据全部就位——AI 三段意见是真的、金额是算出来的、正文里的 `[E1]` 是可点击的证据角标。**先别依赖 `c.onchain`。

---

## 我下一步做

| | |
|---|---|
| D2 wagmi hooks | 不再阻塞（Gate 3 已冻结接口），deadline 走 `deadlineFromNow()` |
| prompt 小改进 | 提醒检方去看责任链的 `sawWarning`。不急，下次改 prompt 时一起 |
